WE LOVE RISHIKESH — Website scaffold

Files created:
- index.html — main static page (mobile-first layout, placeholders for photos)
- styles.css — responsive styling
- assets/placeholder-1.svg, placeholder-2.svg, placeholder-3.svg — image slots

Notes for the user:
- Image slots are marked in HTML with comments like <!-- PHOTO-SLOT: hero --> and use SVG placeholders.
- Replace the SVG files in `assets/` with your real photos. The hero background uses the existing hero image at `hero/hero 2.avif` (left in the workspace).
- I used dummy text throughout — tell me which photo goes into which PHOTO-SLOT and I will swap/label them and update alt text and captions.

Next suggested steps:
1) Map your supplied photos to the PHOTO-SLOT labels.
2) I can produce polished UI copy (microcopy for permit flow), or export simple clickable wireframes.

Namaste — Om Shanti.